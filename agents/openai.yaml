interface:
  display_name: "App Review Status"
  short_description: "Check current App Store review status through the official API"
  default_prompt: "Use $app-review-status to check my App Store Connect review status, and guide me through safe API-key setup if I am not configured yet."
